import numpy as np
import pandas as pd
import scipy as spy
import random as rndm
import math

def rPlotGen():
    i=0
    lidar=[289,391,298,797,699,400,504,508,509]
    np_lidar = np.array(lidar)
    for each in np_lidar:
        print(random.choice(np_lidar))
    rPlotGen()
    print(rPlotGen)

def pyLidar(file, *):
    for each max[] in file:
    return max(height), X, Y
